[package]
name = "durability-tests"
version = "0.1.0"
edition = "2021"
rust-version = "1.74"
publish = false
description = "Crash (kill-mid-write) durability tests for rocksmap. Not published."

[dependencies]
rocksmap = { path = ".." }

[dev-dependencies]
tempfile = "3.8"
